{
  "name": "@dlai-study-notes/tex-to-mdx",
  "description": "Convert course-learning LaTeX (.tex) chapter sources to MDX for the DLAI study notes corpus. Mechanical-only conversion — output is correctable MDX, not publication-ready. Polish happens by hand in MDX during per-book audit pass.",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "tex-to-mdx": "./cli.mjs"
  },
  "scripts": {
    "test": "node --test \"__tests__/*.test.mjs\""
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "unified": "^11.0.5",
    "remark-parse": "^11.0.0",
    "remark-stringify": "^11.0.0",
    "remark-mdx": "^3.1.0",
    "remark-frontmatter": "^5.0.0",
    "yaml": "^2.6.0"
  }
}
