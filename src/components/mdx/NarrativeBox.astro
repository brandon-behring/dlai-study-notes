---
const { title } = Astro.props;
---
<aside class="callout callout--narrative">
  {title && <h4 class="callout-title">{title}</h4>}
  <div class="callout-body"><slot /></div>
</aside>

<style>
  .callout--narrative {
    border-left: 3px solid var(--color-accent, #2b6cb0);
    background: var(--color-callout-narrative, #f0f7ff);
    padding: 1rem 1.25rem;
    margin: 1.5rem 0;
    border-radius: 0 4px 4px 0;
  }
  .callout-title {
    margin: 0 0 0.5rem 0;
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--color-accent, #2b6cb0);
  }
  .callout-body :global(p:last-child) { margin-bottom: 0; }
</style>
