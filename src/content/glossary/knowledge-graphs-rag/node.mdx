---
term: Node
aliases: [vertex]
domain: kg-fundamentals
see: [relationship, label, property]
tags: [graph-data-model]
---

A **data record representing an entity** in a knowledge graph — written `(...)` in Cypher. It
carries one or more [labels](#term-label) (for grouping) and zero or more
[properties](#term-property) (key-value pairs). Equivalent to a graph-theory **vertex**, but
"node" is preferred for data modelling. A `Person` node, a `Company` node, a `Movie` node.
